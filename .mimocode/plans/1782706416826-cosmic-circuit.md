# Plan: ClariPet Assets Implementation (Phase 1: Core Pages) [COMPLETED]

## 1. Goal
Integrate the brand assets (logos, images, and text) from `C:\Nicholas\Website Project\ClariPet Website` into the Next.js project located at `C:\Nicholas\Website Project\testPet`. This initial phase focuses on the **Core Pages & Layout** (Header, Footer, Homepage, About Us, and Contact).

## 2. Approach

We will copy the necessary static files to the `public/` folder in the Next.js project and then modify the React components to use these new images and the text provided in the `.docx` documents.

### Step 2.1: Asset Migration
Using the `bash` tool, copy the following assets from the ClariPet folder to the `testPet` public directory:

- **Branding:** 
  - `1. Logo & Identitas Brand/Claripet_Logo_Dark_Version_HighRes.png` -> `testPet/public/brand/logo-dark.png`
  - `1. Logo & Identitas Brand/Claripet_Logo_Light_Version_HighRes.png` -> `testPet/public/brand/logo-light.png`
  - `1. Logo & Identitas Brand/Favicon.png` -> `testPet/public/favicon.ico`
- **Hero Image:**
  - `5. Aset Gambar & Foto/Foto Lifestyle & Hero/Hero Home Page/Foto Home Page.png` -> `testPet/public/images/hero-home.png`
- **Contact Image:**
  - `2. Informasi Kontak/Informasi Kontak.png` -> `testPet/public/images/contact-info.png`

### Step 2.2: Extract Content
Use the `bash` tool (or Python) to extract plain text from `4. Copywriting\Homepage & About Us.docx` and `4. Copywriting\FAQ Claripet Website.docx` to know exactly what text to inject into the React components.

### Step 2.3: Layout & Navigation Updates
- **`components/Navbar.tsx`**: Update the `Image` src to point to the new logo (`/brand/logo-light.png` or `logo-dark.png` depending on the current theme/design).
- **`components/Footer.tsx`**: Update the logo path here as well, and update any static company text based on the copywriting docs.

### Step 2.4: Core Page Updates
- **`components/home/Hero.tsx`**: Update the background image src to `/images/hero-home.png`. Update the Headline and Subheadline text using the content extracted from the Homepage docx.
- **`app/about/page.tsx`**: Update the static text using the "About Us" section from the copywriting docx.
- **Contact Page / Section**: Integrate the `/images/contact-info.png` into the appropriate Contact component or page.

## 3. Critical Files to Modify
- `C:\Nicholas\Website Project\testPet\components\Navbar.tsx`
- `C:\Nicholas\Website Project\testPet\components\Footer.tsx`
- `C:\Nicholas\Website Project\testPet\components\home\Hero.tsx`
- `C:\Nicholas\Website Project\testPet\app\about\page.tsx` (or related components)
- `C:\Nicholas\Website Project\testPet\app\layout.tsx` (if global meta data / title needs updating)

## 4. Verification
1. Run `npm run dev` in the `testPet` directory.
2. Navigate to `http://localhost:3000` to verify the new Logo is visible in the Navbar.
3. Verify the Hero image and text on the Homepage match the ClariPet assets.
4. Navigate to `/about` and ensure the text is updated.
5. Check for missing image warnings in the terminal or browser console.


---

# Plan: Phase 2 (Product Image Catalog Sync) [COMPLETED]

## 1. Goal
Import the product photography from `C:\Nicholas\Website Project\ClariPet Website\3. Informasi Produk\Photo Produk` into the `testPet/public/images/products` folder and link them up in `C:\Nicholas\Website Project\testPet\data\products.ts` for the 9 products that are missing images (VitaBulu Beauty Powder is excluded as its assets are missing).

## 2. Approach

### Step 2.1: Image Extraction & Renaming
Run a PowerShell script to copy and rename images into a clean, web-safe format using the product slug. 

- Magic Ear Cleaner -> `magic-ear-cleaner.png`, `magic-ear-cleaner-1.png`, etc.
- VitaBulu Spray -> `vitabulu-spray.png`, `vitabulu-spray-1.png`, etc.
- VitaBulu Beauty Shampoo -> `vitabulu-beauty-shampoo.png`, etc.
- Skin Guard Silver Heal -> `skin-guard-silver-heal.png`, etc.
- Skin Guard Fungal Spray -> `skin-guard-fungal-spray.png`, etc.
- Pet Odor Remover Fresh -> `pet-odor-remover-fresh.png`, etc. (using shared base images)
- Pet Odor Remover Sereh -> `pet-odor-remover-sereh.png`, etc. (using shared base images)
- Shu Shu Cat -> `shu-shu-cat.png`, etc. (using shared base images)
- Shu Shu Dog -> `shu-shu-dog.png`, etc. (using shared base images)

### Step 2.2: Update `data/products.ts`
Inject the `images: [{ url: '...', alt: '' }, ...]` array property into the 9 corresponding product objects inside `data/products.ts`. 

## 3. Critical Files
- `C:\Nicholas\Website Project\testPet\data\products.ts`
- Various raw images in the `Photo Produk` folder.

## 4. Verification
- Start the server (`npm run dev`) and visit the `/shop` route to ensure the new images appear without 404 errors.


---

# Plan: Phase 3 (Data Cleanup, Interactive Quiz, & Pet Care Journal) [COMPLETED]

## 1. Goal
Execute the final polish and feature build-out for the testPet project based on selected priorities:
1. **Data Cleanup**: Remove encoding artifacts from content files.
2. **Product Quiz**: Implement the logic to map user answers to actual product recommendations.
3. **Pet Care Journal**: Finalize the blog structure and mock data display.

## 2. Approach

### Step 2.1: Data Cleanup
- Use a Node.js script or manual edit to fix character encoding artifacts across `data/products.ts`, `data/faqs.ts`, and `components/home/Hero.tsx`.
- Target common UTF-8 mismatches and replace them with proper en-dashes, apostrophes, and standard quotes.
- Specifically review `Hero.tsx` for strange symbols in the eyebrow or headline text.

### Step 2.2: Product Quiz Logic
- **File:** `components/quiz/Quiz.tsx`
- Currently, the quiz has steps and state, but the recommendation engine needs to be wired up.
- **Implementation:**
  - Create a matching algorithm between the user's selected concerns and products.
  - Filter products based on chosen pet type (Cat vs Dog).
  - On the final step, calculate the best matching product(s) and display the recommended `ProductCard`.

### Step 2.3: Pet Care Journal (Blog)
- **Files:** `app/journal/page.tsx`, `app/journal/[slug]/page.tsx`, `data/articles.ts`
- **Implementation:**
  - Verify and expand `data/articles.ts` mock data to ensure a robust list of articles.
  - Ensure dynamic routing in `app/journal/[slug]/page.tsx` works correctly.
  - Format the article layout to properly render the `body` paragraphs and `sections` defined in the mock data.

## 3. Critical Files to Modify
- `C:\Nicholas\Website Project\testPet\data\products.ts`
- `C:\Nicholas\Website Project\testPet\data\faqs.ts`
- `C:\Nicholas\Website Project\testPet\components\home\Hero.tsx`
- `C:\Nicholas\Website Project\testPet\components\quiz\Quiz.tsx`
- `C:\Nicholas\Website Project\testPet\data\articles.ts`
- `C:\Nicholas\Website Project\testPet\app\journal\[slug]\page.tsx`

## 4. Verification
1. Run `npm run dev`.
2. Inspect Homepage Hero, FAQs, and Product descriptions to confirm no encoding artifacts remain.
3. Complete the quiz at `/quiz` and verify product recommendations.
4. Visit `/journal` and click into an article to verify the layout and mock content renders correctly.
---

# Plan: Phase 3 (Data Cleanup, Interactive Quiz, & Pet Care Journal) [COMPLETED]

## 1. Goal
Execute the final polish and feature build-out for the testPet project based on selected priorities:
1. **Data Cleanup**: Remove encoding artifacts from content files.
2. **Product Quiz**: Implement the logic to map user answers to actual product recommendations.
3. **Pet Care Journal**: Finalize the blog structure and mock data display.

## 2. Approach

### Step 2.1: Data Cleanup
- Use a Node.js script or manual edit to fix character encoding artifacts across `data/products.ts`, `data/faqs.ts`, and `components/home/Hero.tsx`.
- Target common UTF-8 mismatches and replace them with proper en-dashes, apostrophes, and standard quotes.
- Specifically review `Hero.tsx` for strange symbols in the eyebrow or headline text.

### Step 2.2: Product Quiz Logic
- **File:** `components/quiz/Quiz.tsx`
- Currently, the quiz has steps and state, but the recommendation engine needs to be wired up.
- **Implementation:**
  - Create a matching algorithm between the user's selected concerns and products.
  - Filter products based on chosen pet type (Cat vs Dog).
  - On the final step, calculate the best matching product(s) and display the recommended `ProductCard`.

### Step 2.3: Pet Care Journal (Blog)
- **Files:** `app/journal/page.tsx`, `app/journal/[slug]/page.tsx`, `data/articles.ts`
- **Implementation:**
  - Verify and expand `data/articles.ts` mock data to ensure a robust list of articles.
  - Ensure dynamic routing in `app/journal/[slug]/page.tsx` works correctly.
  - Format the article layout to properly render the `body` paragraphs and `sections` defined in the mock data.

## 3. Critical Files to Modify
- `C:\Nicholas\Website Project\testPet\data\products.ts`
- `C:\Nicholas\Website Project\testPet\data\faqs.ts`
- `C:\Nicholas\Website Project\testPet\components\home\Hero.tsx`
- `C:\Nicholas\Website Project\testPet\components\quiz\Quiz.tsx`
- `C:\Nicholas\Website Project\testPet\data\articles.ts`
- `C:\Nicholas\Website Project\testPet\app\journal\[slug]\page.tsx`

## 4. Verification
1. Run `npm run dev`.
2. Inspect Homepage Hero, FAQs, and Product descriptions to confirm no encoding artifacts remain.
3. Complete the quiz at `/quiz` and verify product recommendations.
4. Visit `/journal` and click into an article to verify the layout and mock content renders correctly.
